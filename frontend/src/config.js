// Base configuration for SnapAttend frontend

export const API_BASE_URL = "http://localhost:8080/api";

// Where we store auth info in localStorage
export const TOKEN_KEY = "snapattend_token";
export const USER_KEY = "snapattend_user";
